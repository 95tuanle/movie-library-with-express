{{#if this.errors}}
    {{#each errors}}
        <script>
            $('#{{this.param}}')[0].classList.add('is-invalid')
            $('#{{this.param}}-error')[0].innerText = "{{this.msg}}"
        </script>
    {{/each}}
{{/if}}